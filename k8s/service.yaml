apiVersion: v1
kind: Service
metadata:
  name: talesofchina-lb
  labels:
    app: talesofchina
spec:
  type: LoadBalancer
  selector:
    app: talesofchina
  ports:
    - name: http
      port: 80          # Port exposed on LoadBalancer
      targetPort: 80    # Container port in your pod
